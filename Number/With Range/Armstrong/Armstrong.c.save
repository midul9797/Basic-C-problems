#include<stdio.h>

int main(){
    int n,remainder,digit,main, count, a,b,k, sum;

    scanf("%d %d", &a, &b);
    for(int i =a; i<=b; i++){
            sum =0;
    count=0;
    n=i;
    main = i;

    while(n>0){
        digit = n % 10;
        n/=10;
        count++;
    }
    while(n>0){
        remainder = n%10;
        sum = sum + pow(remainder, count);
        n/=10;
    }
    if(main == 0)
        printf("not");
    else{

    if(main == sum)
        printf("%d Armstrong\n", main);
    else
        printf("not");
    }
    }
    return 0;
}
